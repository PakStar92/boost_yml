import { EventEmitter } from 'events';
import type { ProgressInfo } from '../types/progress.js';
/**
 * All events emitted by an FFmpegProcess.
 */
export interface FFmpegEvents {
    /** Raw stderr line */
    stderr: [line: string];
    /** Parsed progress update (emitted on each progress=continue|end block) */
    progress: [info: ProgressInfo];
    /** Process started — emits the full args array */
    start: [args: string[]];
    /** Process finished successfully */
    end: [];
    /** Process failed */
    error: [err: Error];
}
/**
 * Typed EventEmitter for FFmpeg process events.
 */
export declare class FFmpegEmitter extends EventEmitter {
    on<K extends keyof FFmpegEvents>(event: K, listener: (...args: FFmpegEvents[K]) => void): this;
    once<K extends keyof FFmpegEvents>(event: K, listener: (...args: FFmpegEvents[K]) => void): this;
    off<K extends keyof FFmpegEvents>(event: K, listener: (...args: FFmpegEvents[K]) => void): this;
    emit<K extends keyof FFmpegEvents>(event: K, ...args: FFmpegEvents[K]): boolean;
}
//# sourceMappingURL=events.d.ts.map