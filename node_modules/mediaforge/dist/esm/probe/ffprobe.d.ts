import type { ProbeResult, ProbeStream, ParsedFrameRate, VideoStreamSummary, AudioStreamSummary } from '../types/probe.js';
export interface ProbeOptions {
    /** Path to ffprobe binary (default: FFPROBE_PATH env or 'ffprobe') */
    binary?: string;
    /** Include chapter information. Default: true */
    chapters?: boolean;
    /** Timeout in ms. Default: 30000 */
    timeout?: number;
    /** Extra ffprobe args to pass */
    extraArgs?: string[];
}
/**
 * Run ffprobe on a file and return fully typed JSON output.
 * Synchronous version — use `probeAsync` for non-blocking operation.
 *
 * @example
 * const info = probe('input.mp4');
 * console.log(info.format?.duration); // "120.042000"
 * console.log(info.streams[0].codec_name); // "h264"
 */
export declare function probe(filePath: string, opts?: ProbeOptions): ProbeResult;
/**
 * Async version of probe — non-blocking, suitable for server use.
 */
export declare function probeAsync(filePath: string, opts?: ProbeOptions): Promise<ProbeResult>;
export declare class ProbeError extends Error {
    readonly filePath: string;
    readonly detail: string;
    constructor(filePath: string, detail: string);
}
/**
 * Parse an ffprobe fractional frame rate string.
 * e.g. "30000/1001" → { num:30000, den:1001, value:29.97 }
 * e.g. "25/1"       → { num:25,    den:1,    value:25 }
 *
 * @returns ParsedFrameRate object, or null if the string is absent/invalid.
 */
export declare function parseFrameRate(frStr: string | undefined): ParsedFrameRate | null;
/**
 * Parse a duration string from ffprobe (e.g. "120.042000") to a number.
 * Returns null if the value is absent or "N/A".
 */
export declare function parseDuration(durStr: string | undefined): number | null;
/**
 * Parse a bitrate string (e.g. "4200000") to a number.
 */
export declare function parseBitrate(brStr: string | undefined): number | null;
/**
 * Get all video streams from a ProbeResult.
 */
export declare function getVideoStreams(result: ProbeResult): ProbeStream[];
/**
 * Get all audio streams from a ProbeResult.
 */
export declare function getAudioStreams(result: ProbeResult): ProbeStream[];
/**
 * Get all subtitle streams from a ProbeResult.
 */
export declare function getSubtitleStreams(result: ProbeResult): ProbeStream[];
/**
 * Get the first video stream, or null if none exists.
 */
export declare function getDefaultVideoStream(result: ProbeResult): ProbeStream | null;
/**
 * Get the first audio stream, or null if none exists.
 */
export declare function getDefaultAudioStream(result: ProbeResult): ProbeStream | null;
/**
 * Return the total duration of the media in seconds.
 * Prefers format.duration, falls back to the first stream with a duration.
 */
export declare function getMediaDuration(result: ProbeResult): number | null;
/**
 * Convert total duration in seconds to microseconds (for progress calculation).
 */
export declare function durationToMicroseconds(seconds: number): number;
/**
 * Build a human-readable summary of a video stream.
 */
export declare function summarizeVideoStream(s: ProbeStream): VideoStreamSummary;
/**
 * Build a human-readable summary of an audio stream.
 */
export declare function summarizeAudioStream(s: ProbeStream): AudioStreamSummary;
/**
 * Get a stream's language tag, or null if not set.
 */
export declare function getStreamLanguage(s: ProbeStream): string | null;
/**
 * Find a stream by language tag (case-insensitive).
 * Returns the first match, or null.
 */
export declare function findStreamByLanguage(result: ProbeResult, lang: string, codecType?: 'video' | 'audio' | 'subtitle'): ProbeStream | null;
/**
 * Format a duration in seconds to a human-readable HH:MM:SS.mmm string.
 */
export declare function formatDuration(seconds: number): string;
/**
 * Check if a file appears to be HDR (BT.2020 primaries or PQ/HLG transfer).
 */
export declare function isHdr(result: ProbeResult): boolean;
/**
 * Check if a file's video stream is interlaced.
 */
export declare function isInterlaced(result: ProbeResult): boolean;
/**
 * Get all chapter titles and times as a simple array.
 */
export declare function getChapterList(result: ProbeResult): Array<{
    title: string;
    startSec: number;
    endSec: number;
}>;
//# sourceMappingURL=ffprobe.d.ts.map