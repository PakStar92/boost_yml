export interface TwoPassOptions {
    input: string;
    output: string;
    videoCodec: string;
    videoBitrate: string;
    audioCodec?: string;
    audioBitrate?: string;
    passlogfile?: string;
    binary?: string;
    extraOutputArgs?: string[];
    extraInputArgs?: string[];
    overwrite?: boolean;
    onPass1Complete?: () => void;
    onPass2Complete?: () => void;
}
/**
 * Run a two-pass encode for the given codec.
 * Pass 1: analyse → generate stats file. Pass 2: encode with stats.
 *
 * @example
 * await twoPassEncode({
 *   input: 'input.mp4',
 *   output: 'output.mp4',
 *   videoCodec: 'libx264',
 *   videoBitrate: '2M',
 *   audioCodec: 'aac',
 *   audioBitrate: '128k',
 * });
 */
export declare function twoPassEncode(opts: TwoPassOptions): Promise<void>;
/**
 * Build pass 1 and pass 2 argument arrays without running them.
 * Useful for manual orchestration or testing.
 */
export declare function buildTwoPassArgs(opts: TwoPassOptions): {
    pass1: string[];
    pass2: string[];
    passlog: string;
};
//# sourceMappingURL=twopass.d.ts.map