export interface BurnSubtitlesOptions {
    /** Input video */
    input: string;
    /** Subtitle file (.srt, .ass, .vtt, etc.) */
    subtitleFile: string;
    /** Output file */
    output: string;
    /** Font size override */
    fontSize?: number;
    /** Font name override */
    fontName?: string;
    /** Primary color (ASS format, e.g. '&H00FFFFFF&') */
    primaryColor?: string;
    /** Video codec. Default: 'libx264' */
    videoCodec?: string;
    /** ffmpeg binary override */
    binary?: string;
}
/**
 * Burn (hardcode) subtitles into a video.
 *
 * @example
 * await burnSubtitles({
 *   input: 'video.mp4',
 *   subtitleFile: 'subs.srt',
 *   output: 'video-subbed.mp4',
 *   fontSize: 24,
 * });
 */
export declare function burnSubtitles(opts: BurnSubtitlesOptions): Promise<void>;
export interface ExtractSubtitlesOptions {
    /** Input file */
    input: string;
    /** Output subtitle file (.srt, .ass, etc.) */
    output: string;
    /** Stream index. Default: 0 */
    streamIndex?: number;
    /** ffmpeg binary override */
    binary?: string;
}
/**
 * Extract a subtitle stream from a container to a file.
 *
 * @example
 * await extractSubtitles({ input: 'video.mkv', output: 'subs.srt' });
 */
export declare function extractSubtitles(opts: ExtractSubtitlesOptions): Promise<void>;
export declare function buildBurnSubtitlesFilter(subtitleFile: string, fontSize?: number, fontName?: string, primaryColor?: string): string;
//# sourceMappingURL=subtitles.d.ts.map