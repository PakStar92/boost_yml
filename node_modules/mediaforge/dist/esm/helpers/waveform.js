import { runFFmpeg } from '../process/spawn.js';
import { resolveBinary } from '../utils/binary.js';
/**
 * Generate a waveform image from an audio or video file.
 *
 * @example
 * await generateWaveform({
 *   input: 'audio.mp3',
 *   output: 'waveform.png',
 *   width: 1920, height: 240,
 *   color: '#00aaff',
 * });
 */
export async function generateWaveform(opts) {
    const { input, output, width = 1920, height = 240, color = '#00ff00', scale = 'lin', streamIndex = 0, binary = resolveBinary(), } = opts;
    // FFmpeg 7.x+ removed bgcolor and draw from showwavespic.
    // Only emit s= (size) and colors= — these are stable across v6/v7/v8.
    const filter = `[0:a:${streamIndex}]showwavespic=s=${width}x${height}:colors=${color}:scale=${scale}[v]`;
    const args = [
        '-y', '-i', input,
        '-filter_complex', filter,
        '-map', '[v]',
        '-frames:v', '1',
        output,
    ];
    await runFFmpeg({ binary, args });
}
export async function generateSpectrum(opts) {
    const { input, output, width = 1280, height = 720, color = 'fire', fps = 25, binary = resolveBinary(), } = opts;
    const filter = `showspectrum=s=${width}x${height}:color=${color}:fps=${fps}:mode=combined`;
    await runFFmpeg({
        binary,
        args: [
            '-y', '-i', input,
            '-filter_complex', `[0:a]${filter}[v]`,
            '-map', '[v]',
            '-map', '0:a',
            '-c:v', 'libx264', '-c:a', 'aac',
            output,
        ],
    });
}
// ─── Arg builders ─────────────────────────────────────────────────────────────
export function buildWaveformFilter(width, height, color, scale, _mode, streamIndex, _backgroundColor) {
    // bgcolor and draw removed in FFmpeg 7.x+ — never emit them
    return `[0:a:${streamIndex}]showwavespic=s=${width}x${height}:colors=${color}:scale=${scale}[v]`;
}
export function buildSpectrumFilter(width, height, color, fps) {
    return `showspectrum=s=${width}x${height}:color=${color}:fps=${fps}:mode=combined`;
}
//# sourceMappingURL=waveform.js.map