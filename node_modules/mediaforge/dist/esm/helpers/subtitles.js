import { FFmpegBuilder } from '../FFmpeg.js';
import { runFFmpeg } from '../process/spawn.js';
import { resolveBinary } from '../utils/binary.js';
/**
 * Burn (hardcode) subtitles into a video.
 *
 * @example
 * await burnSubtitles({
 *   input: 'video.mp4',
 *   subtitleFile: 'subs.srt',
 *   output: 'video-subbed.mp4',
 *   fontSize: 24,
 * });
 */
export async function burnSubtitles(opts) {
    const { input, subtitleFile, output, fontSize, fontName, primaryColor, videoCodec = 'libx264', binary = resolveBinary(), } = opts;
    // Build subtitle filter with optional style overrides
    const escapedPath = subtitleFile.replace(/\\/g, '/').replace(/:/g, '\\:').replace(/'/g, "\\'");
    let filter = `subtitles='${escapedPath}'`;
    const styles = [];
    if (fontSize)
        styles.push(`FontSize=${fontSize}`);
    if (fontName)
        styles.push(`FontName=${fontName}`);
    if (primaryColor)
        styles.push(`PrimaryColour=${primaryColor}`);
    if (styles.length > 0)
        filter += `:force_style='${styles.join(',')}'`;
    await new FFmpegBuilder(input)
        .output(output)
        .videoFilter(filter)
        .videoCodec(videoCodec)
        .audioCodec('copy')
        .setBinary(binary)
        .run();
}
/**
 * Extract a subtitle stream from a container to a file.
 *
 * @example
 * await extractSubtitles({ input: 'video.mkv', output: 'subs.srt' });
 */
export async function extractSubtitles(opts) {
    const { input, output, streamIndex = 0, binary = resolveBinary() } = opts;
    await runFFmpeg({
        binary,
        args: ['-y', '-i', input, '-map', `0:s:${streamIndex}`, output],
    });
}
// ─── Arg builders ─────────────────────────────────────────────────────────────
export function buildBurnSubtitlesFilter(subtitleFile, fontSize, fontName, primaryColor) {
    const escapedPath = subtitleFile.replace(/\\/g, '/').replace(/:/g, '\\:').replace(/'/g, "\\'");
    let filter = `subtitles='${escapedPath}'`;
    const styles = [];
    if (fontSize)
        styles.push(`FontSize=${fontSize}`);
    if (fontName)
        styles.push(`FontName=${fontName}`);
    if (primaryColor)
        styles.push(`PrimaryColour=${primaryColor}`);
    if (styles.length > 0)
        filter += `:force_style='${styles.join(',')}'`;
    return filter;
}
//# sourceMappingURL=subtitles.js.map