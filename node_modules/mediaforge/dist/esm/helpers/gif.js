import * as path from 'path';
import * as os from 'os';
import * as fs from 'fs';
import { runFFmpeg } from '../process/spawn.js';
import { resolveBinary } from '../utils/binary.js';
/**
 * Convert video to a high-quality animated GIF using the palette method.
 * Two-pass: first generates an optimal palette, then uses it to dither.
 *
 * @example
 * await toGif({ input: 'clip.mp4', output: 'clip.gif', width: 480, fps: 15 });
 */
export async function toGif(opts) {
    const { input, output, width = 480, fps = 15, startTime, duration, colors = 256, dither = 'bayer', binary = resolveBinary(), } = opts;
    const palettePath = path.join(os.tmpdir(), `palette-${Date.now()}.png`);
    const scale = `fps=${fps},scale=${width}:-1:flags=lanczos`;
    try {
        // Pass 1: generate palette
        const pass1Args = ['-y'];
        if (startTime !== undefined)
            pass1Args.push('-ss', String(startTime));
        if (duration !== undefined)
            pass1Args.push('-t', String(duration));
        pass1Args.push('-i', input, '-vf', `${scale},palettegen=max_colors=${colors}`, palettePath);
        await runFFmpeg({ binary, args: pass1Args });
        // Pass 2: apply palette
        const pass2Args = ['-y'];
        if (startTime !== undefined)
            pass2Args.push('-ss', String(startTime));
        if (duration !== undefined)
            pass2Args.push('-t', String(duration));
        pass2Args.push('-i', input, '-i', palettePath, '-lavfi', `${scale} [x]; [x][1:v] paletteuse=dither=${dither}`, output);
        await runFFmpeg({ binary, args: pass2Args });
    }
    finally {
        if (fs.existsSync(palettePath))
            fs.unlinkSync(palettePath);
    }
}
export async function gifToMp4(opts) {
    const { input, output, width, binary = resolveBinary() } = opts;
    const args = ['-y', '-i', input];
    const filters = ['scale=trunc(iw/2)*2:trunc(ih/2)*2'];
    if (width)
        filters.unshift(`scale=${width}:-2:flags=lanczos`);
    args.push('-vf', filters.join(','), '-c:v', 'libx264', '-pix_fmt', 'yuv420p', '-movflags', '+faststart', '-an', output);
    await runFFmpeg({ binary, args });
}
// ─── Arg builders ─────────────────────────────────────────────────────────────
export function buildGifPalettegenFilter(fps, width, colors) {
    return `fps=${fps},scale=${width}:-1:flags=lanczos,palettegen=max_colors=${colors}`;
}
export function buildGifPaletteuseFilter(fps, width, dither) {
    const scale = `fps=${fps},scale=${width}:-1:flags=lanczos`;
    return `${scale} [x]; [x][1:v] paletteuse=dither=${dither}`;
}
export function buildGifArgs(input, palettePath, output, fps, width, dither, startTime, duration) {
    const pass1 = ['-y'];
    if (startTime !== undefined)
        pass1.push('-ss', String(startTime));
    if (duration !== undefined)
        pass1.push('-t', String(duration));
    const scale = `fps=${fps},scale=${width}:-1:flags=lanczos`;
    pass1.push('-i', input, '-vf', `${scale},palettegen`, palettePath);
    const pass2 = ['-y'];
    if (startTime !== undefined)
        pass2.push('-ss', String(startTime));
    if (duration !== undefined)
        pass2.push('-t', String(duration));
    pass2.push('-i', input, '-i', palettePath, '-lavfi', `${scale} [x]; [x][1:v] paletteuse=dither=${dither}`, output);
    return { pass1, pass2 };
}
//# sourceMappingURL=gif.js.map