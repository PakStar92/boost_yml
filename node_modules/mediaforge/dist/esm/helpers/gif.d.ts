export interface GifOptions {
    /** Input file */
    input: string;
    /** Output GIF path */
    output: string;
    /** Output width. Height auto-scales. Default: 480 */
    width?: number;
    /** Frames per second. Default: 15 */
    fps?: number;
    /** Start time */
    startTime?: string | number;
    /** Duration in seconds */
    duration?: string | number;
    /** Number of colors (2-256). Default: 256 */
    colors?: number;
    /** Dither mode. Default: 'bayer' */
    dither?: 'bayer' | 'floyd_steinberg' | 'sierra2' | 'sierra2_4a' | 'none';
    /** ffmpeg binary override */
    binary?: string;
}
/**
 * Convert video to a high-quality animated GIF using the palette method.
 * Two-pass: first generates an optimal palette, then uses it to dither.
 *
 * @example
 * await toGif({ input: 'clip.mp4', output: 'clip.gif', width: 480, fps: 15 });
 */
export declare function toGif(opts: GifOptions): Promise<void>;
/**
 * Convert GIF to MP4 (useful for uploading to platforms that don't accept GIF).
 *
 * @example
 * await gifToMp4({ input: 'animation.gif', output: 'animation.mp4' });
 */
export interface GifToMp4Options {
    input: string;
    output: string;
    /** Output width. Default: source width */
    width?: number;
    /** ffmpeg binary override */
    binary?: string;
}
export declare function gifToMp4(opts: GifToMp4Options): Promise<void>;
export declare function buildGifPalettegenFilter(fps: number, width: number, colors: number): string;
export declare function buildGifPaletteuseFilter(fps: number, width: number, dither: string): string;
export declare function buildGifArgs(input: string, palettePath: string, output: string, fps: number, width: number, dither: string, startTime?: string | number, duration?: string | number): {
    pass1: string[];
    pass2: string[];
};
//# sourceMappingURL=gif.d.ts.map