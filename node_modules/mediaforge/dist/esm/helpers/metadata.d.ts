export interface WriteMetadataOptions {
    /** Input file */
    input: string;
    /** Output file */
    output: string;
    /** Container-level metadata */
    metadata: Record<string, string>;
    /** Per-stream metadata: key is stream specifier e.g. 'v:0', 'a:0', 's:0' */
    streamMetadata?: Record<string, Record<string, string>>;
    /** Chapter definitions */
    chapters?: ChapterMeta[];
    /** ffmpeg binary override */
    binary?: string;
}
export interface ChapterMeta {
    /** Chapter title */
    title: string;
    /** Start time in seconds */
    startSec: number;
    /** End time in seconds */
    endSec: number;
}
/**
 * Write metadata tags to a file without re-encoding.
 *
 * @example
 * await writeMetadata({
 *   input: 'video.mp4',
 *   output: 'tagged.mp4',
 *   metadata: { title: 'My Film', artist: 'Director', year: '2025' },
 * });
 */
export declare function writeMetadata(opts: WriteMetadataOptions): Promise<void>;
/**
 * Strip ALL metadata from a file (privacy-safe export).
 *
 * @example
 * await stripMetadata({ input: 'original.mp4', output: 'clean.mp4' });
 */
export interface StripMetadataOptions {
    input: string;
    output: string;
    binary?: string;
}
export declare function stripMetadata(opts: StripMetadataOptions): Promise<void>;
export declare function buildMetadataArgs(metadata: Record<string, string>, streamMetadata?: Record<string, Record<string, string>>): string[];
export declare function buildChapterContent(chapters: ChapterMeta[]): string;
//# sourceMappingURL=metadata.d.ts.map