export interface NormalizeOptions {
    input: string;
    output: string;
    /** Target integrated loudness in LUFS. Default: -23 (EBU R128) */
    targetI?: number;
    /** Target loudness range in LU. Default: 7 */
    targetLra?: number;
    /** Target true peak in dBTP. Default: -2 */
    targetTp?: number;
    /** Two-pass for accuracy. Default: true */
    twoPass?: boolean;
    /** Video codec. Default: 'copy' */
    videoCodec?: string;
    binary?: string;
}
export interface NormalizeResult {
    inputI: number;
    inputLra: number;
    inputTp: number;
    inputThresh: number;
    targetOffset: number;
}
/**
 * Normalize audio loudness (EBU R128 / ITU-R BS.1770).
 *
 * @example
 * const result = await normalizeAudio({ input: 'raw.mp4', output: 'norm.mp4', targetI: -16 });
 */
export declare function normalizeAudio(opts: NormalizeOptions): Promise<NormalizeResult>;
export interface AdjustVolumeOptions {
    input: string;
    output: string;
    volume: string;
    videoCodec?: string;
    binary?: string;
}
/**
 * Adjust volume by a multiplier or dB value.
 * @example
 * await adjustVolume({ input: 'in.mp4', output: 'out.mp4', volume: '0.5' });
 * await adjustVolume({ input: 'in.mp4', output: 'out.mp4', volume: '6dB' });
 */
export declare function adjustVolume(opts: AdjustVolumeOptions): Promise<void>;
export declare function buildLoudnormFilter(targetI: number, targetLra: number, targetTp: number, measured?: {
    inputI: number;
    inputLra: number;
    inputTp: number;
    inputThresh: number;
    targetOffset: number;
}): string;
//# sourceMappingURL=normalize.d.ts.map