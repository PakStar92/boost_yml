export type WatermarkPosition = 'top-left' | 'top-right' | 'top-center' | 'bottom-left' | 'bottom-right' | 'bottom-center' | 'center' | string;
export interface WatermarkOptions {
    /** Main video input */
    input: string;
    /** Watermark image path (PNG with transparency recommended) */
    watermark: string;
    /** Output file */
    output: string;
    /** Position. Default: 'bottom-right' */
    position?: WatermarkPosition;
    /** Margin from edges in pixels. Default: 10 */
    margin?: number;
    /** Opacity 0-1. Default: 1 */
    opacity?: number;
    /** Scale watermark to this width (height auto). Optional */
    scaleWidth?: number;
    /** Video codec. Default: 'libx264' */
    videoCodec?: string;
    /** Audio codec. Default: 'copy' */
    audioCodec?: string;
    /** ffmpeg binary override */
    binary?: string;
}
/**
 * Add an image watermark to a video.
 *
 * @example
 * await addWatermark({
 *   input: 'video.mp4',
 *   watermark: 'logo.png',
 *   output: 'watermarked.mp4',
 *   position: 'bottom-right',
 *   opacity: 0.7,
 * });
 */
export declare function addWatermark(opts: WatermarkOptions): Promise<void>;
/**
 * Add a text watermark to a video.
 *
 * @example
 * await addTextWatermark({
 *   input: 'video.mp4',
 *   output: 'watermarked.mp4',
 *   text: '© MyCompany 2025',
 *   position: 'bottom-right',
 *   fontSize: 24,
 *   fontColor: 'white@0.7',
 * });
 */
export interface TextWatermarkOptions {
    input: string;
    output: string;
    text: string;
    position?: WatermarkPosition;
    margin?: number;
    fontSize?: number;
    fontColor?: string;
    fontFile?: string;
    videoCodec?: string;
    audioCodec?: string;
    binary?: string;
}
export declare function addTextWatermark(opts: TextWatermarkOptions): Promise<void>;
export declare function buildWatermarkFilter(position: WatermarkPosition, margin: number, opacity: number, scaleWidth?: number): string;
export declare function buildTextWatermarkFilter(text: string, position: WatermarkPosition, margin: number, fontSize: number, fontColor: string, fontFile?: string): string;
//# sourceMappingURL=watermark.d.ts.map