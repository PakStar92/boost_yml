export interface WaveformOptions {
    /** Input audio/video file */
    input: string;
    /** Output image path (.png) */
    output: string;
    /** Image width in pixels. Default: 1920 */
    width?: number;
    /** Image height in pixels. Default: 240 */
    height?: number;
    /** Waveform color. Default: '#00ff00' */
    color?: string;
    /**
     * Background color.
     * @deprecated FFmpeg 7.x+ removed the bgcolor parameter from showwavespic.
     * This option is accepted for API compatibility but has no effect.
     */
    backgroundColor?: string;
    /**
     * Drawing mode: 'line', 'point', 'p2p', 'cline'.
     * @deprecated FFmpeg 7.x+ removed the draw parameter from showwavespic.
     * This option is accepted for API compatibility but has no effect.
     */
    mode?: 'line' | 'point' | 'p2p' | 'cline';
    /** Display scale: 'lin' or 'log'. Default: 'lin' */
    scale?: 'lin' | 'log';
    /** Audio stream index. Default: 0 */
    streamIndex?: number;
    /** ffmpeg binary override */
    binary?: string;
}
/**
 * Generate a waveform image from an audio or video file.
 *
 * @example
 * await generateWaveform({
 *   input: 'audio.mp3',
 *   output: 'waveform.png',
 *   width: 1920, height: 240,
 *   color: '#00aaff',
 * });
 */
export declare function generateWaveform(opts: WaveformOptions): Promise<void>;
/**
 * Generate a real-time audio spectrum visualizer video.
 * Useful for podcasts, music visualizations.
 *
 * @example
 * await generateSpectrum({
 *   input: 'podcast.mp3',
 *   output: 'spectrum.mp4',
 *   width: 1280, height: 720,
 * });
 */
export interface SpectrumOptions {
    input: string;
    output: string;
    width?: number;
    height?: number;
    color?: string;
    fps?: number;
    binary?: string;
}
export declare function generateSpectrum(opts: SpectrumOptions): Promise<void>;
export declare function buildWaveformFilter(width: number, height: number, color: string, scale: string, _mode: string, streamIndex: number, _backgroundColor?: string): string;
export declare function buildSpectrumFilter(width: number, height: number, color: string, fps: number): string;
//# sourceMappingURL=waveform.d.ts.map