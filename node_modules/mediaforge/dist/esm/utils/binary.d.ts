export declare class BinaryNotFoundError extends Error {
    constructor(binary: string);
}
export declare class BinaryNotExecutableError extends Error {
    constructor(binary: string);
}
/**
 * Resolve the ffmpeg binary path.
 * Priority: explicit path → FFMPEG_PATH env → 'ffmpeg' on PATH
 */
export declare function resolveBinary(explicit?: string): string;
/**
 * Resolve the ffprobe binary path.
 * Priority: explicit path → FFPROBE_PATH env → 'ffprobe' on PATH
 */
export declare function resolveProbe(explicit?: string): string;
/**
 * Validate that a binary exists and is executable.
 * Throws BinaryNotFoundError or BinaryNotExecutableError on failure.
 */
export declare function validateBinary(binaryPath: string): void;
/**
 * Return true if the binary can be found and executed.
 */
export declare function isBinaryAvailable(binaryPath: string): boolean;
//# sourceMappingURL=binary.d.ts.map