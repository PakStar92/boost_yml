import type { VersionInfo } from '../types/version.js';
/**
 * Parse the raw text output of `ffmpeg -version`.
 */
export declare function parseVersionOutput(output: string): VersionInfo;
/**
 * Probe the given binary and return parsed VersionInfo.
 * Throws if the binary cannot be executed.
 */
export declare function probeVersion(binaryPath: string): VersionInfo;
/**
 * Return true if actual satisfies the requirement (major >= min, minor >= minMinor within same major).
 */
export declare function satisfiesVersion(actual: Pick<VersionInfo, 'major' | 'minor'>, minMajor: number, minMinor?: number): boolean;
/**
 * Format a VersionInfo back to a human-readable string.
 */
export declare function formatVersion(v: VersionInfo): string;
//# sourceMappingURL=version.d.ts.map