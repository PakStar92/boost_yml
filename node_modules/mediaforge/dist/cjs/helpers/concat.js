"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.mergeToFile = mergeToFile;
exports.concatFiles = concatFiles;
exports.buildConcatList = buildConcatList;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const os = __importStar(require("os"));
const spawn_js_1 = require("../process/spawn.js");
const binary_js_1 = require("../utils/binary.js");
/**
 * Concatenate multiple video/audio files into one using the concat demuxer.
 * This is the fastest method (stream copy by default, no re-encode).
 *
 * @example
 * await mergeToFile({
 *   inputs: ['part1.mp4', 'part2.mp4', 'part3.mp4'],
 *   output: 'merged.mp4',
 * });
 */
async function mergeToFile(opts) {
    const { inputs, output, reencode = false, videoCodec = 'libx264', audioCodec = 'aac', extraArgs = [], binary = (0, binary_js_1.resolveBinary)(), } = opts;
    if (inputs.length === 0)
        throw new Error('mergeToFile: no inputs provided');
    if (inputs.length === 1) {
        fs.copyFileSync(inputs[0], output);
        return;
    }
    // Write concat list file
    const tmpList = path.join(os.tmpdir(), `ffmpeg-concat-${Date.now()}.txt`);
    const listContent = inputs
        .map(f => `file '${path.resolve(f).replace(/'/g, "'\\''")}'`)
        .join('\n');
    fs.writeFileSync(tmpList, listContent);
    try {
        const args = ['-y', '-f', 'concat', '-safe', '0', '-i', tmpList];
        if (reencode) {
            args.push('-c:v', videoCodec, '-c:a', audioCodec);
        }
        else {
            args.push('-c', 'copy');
        }
        args.push(...extraArgs, output);
        await (0, spawn_js_1.runFFmpeg)({ binary, args });
    }
    finally {
        fs.unlinkSync(tmpList);
    }
}
/**
 * Build a concat filter_complex for re-encoding concatenation with optional transitions.
 * Returns an FFmpegProcess for event-based control.
 */
function concatFiles(opts) {
    const { inputs, output, binary = (0, binary_js_1.resolveBinary)(), } = opts;
    // Simple concat filter approach
    const n = inputs.length;
    const inputArgs = [];
    for (const inp of inputs)
        inputArgs.push('-i', inp);
    let filterComplex = '';
    for (let i = 0; i < n; i++)
        filterComplex += `[${i}:v][${i}:a]`;
    filterComplex += `concat=n=${n}:v=1:a=1[v][a]`;
    const args = [
        '-y',
        ...inputArgs,
        '-filter_complex', filterComplex,
        '-map', '[v]',
        '-map', '[a]',
        '-c:v', 'libx264',
        '-c:a', 'aac',
        output,
    ];
    return (0, spawn_js_1.spawnFFmpeg)({ binary, args });
}
/**
 * Build the concat demuxer file content without running ffmpeg.
 * Useful for inspection or custom piping.
 */
function buildConcatList(files) {
    return files
        .map(f => `file '${path.resolve(f).replace(/'/g, "'\\''")}'`)
        .join('\n');
}
